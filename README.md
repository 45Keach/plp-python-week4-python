# Week 4 Assignment: Hands-On Lab - Grades, Eligibility & Smart Decisions

- `welcome.py` — Defines and uses a `welcome()` function to return greetings for three names.
- `toolbox.py` — Defines `double()`, `is_pass()`, and `greet()` functions and tests them.

The hardest function to write was `greet()` because it uses a default parameter while also building the greeting sentence.
